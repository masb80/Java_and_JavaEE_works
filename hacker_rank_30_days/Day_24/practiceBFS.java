package hacker_rank_30_days.Day_24;

public class practiceBFS {
   // n = number of nodes in the graph;
   // g = adjacent list representing unweighted graph;
   //  s = start node, e = end node; and 0 <= e, s < n;

    // function bfs(s,e);
      //do a BFS starting at node
   //   prev = solve(s);

      // Return recostructed path from s -> e 
   //    return reconstruntPath(s,e,prev);
    public static void main(String[] args) {
        
    }
    
}
